export enum CrytonRESTApiEndpoint {
  RUNS = 'runs/',
  EXECUTION_VARS = 'execution_variables/',
  PLAN_EXECUTIONS = 'plan_executions',
  INSTANCES = 'plans/',
  LOGS = 'logs/',
  TEMPLATES = 'templates/',
  WORKERS = 'workers/'
}
