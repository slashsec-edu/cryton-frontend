export enum Endpoint {
  RUNS = 'runs/',
  EXECUTION_VARS = 'execution_variables/',
  PLAN_EXECUTIONS = 'plan_executions',
  PLANS = 'plans/',
  LOGS = 'logs/',
  TEMPLATES = 'templates/',
  WORKERS = 'workers/'
}
