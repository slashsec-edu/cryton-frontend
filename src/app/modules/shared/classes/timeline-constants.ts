/**
 * Defines the width of an area from one tick to the other.
 */
export const TICK_WIDTH = 20;

/**
 * How far from each other the leading ticks are.
 */
export const LTICK_DIST = 10;

/**
 * Defines how many ticks should be pre-rendered on the sides of the timeline.
 */
export const RECYCLER_RADIUS = 10;

/**
 * Defines how many ticks from parent node should the HTTP node be rendered.
 */
export const HTTP_NODE_TICK_DIST = 5;
